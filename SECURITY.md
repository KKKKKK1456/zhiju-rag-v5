# 安全和隐私边界

这是本机、单用户、有限并发的体验工具，不是多租户服务。HTTP 只绑定回环地址，并校验 Host / Origin / 会话标记；会话标记不是公网身份认证。不要公网暴露 8772，不要分享 Docker socket，不要把数据库直接暴露给朋友。

发布包不含知识库、原文、向量/索引、模型权重、账户凭据、聊天、日志、旧评测结果、个人绝对路径或原环境资源 ID。只按源码白名单打包；模型和知识库由接收方单独准备。

默认仅本地检索。`--cloud` 只开启页面许可选项，不提交请求；每次回答仍需页面明确许可。许可后问题和有限证据发给 Moonshot。法律文档或提问可包含敏感个人信息，需由资料所有者判断是否允许外发。导出结果同样可能敏感。

不要提交 `.env`、密钥、数据库备份、结果 JSON、截图、日志或私有法规集合。即使 `.gitignore` 存在，也可能被 `git add -f` 绕过。每次分享前运行 `python -m tools.package_release --check` 并人工复核新增文件。模式扫描不是对任何密钥格式的完全证明。

若密钥已经进入任何远端提交或公开下载：先到服务商撤销/轮换密钥，再清理提交历史与下载物；只删最新文件不能撤销历史泄露。不要在公开 issue 中贴密钥或原始聊天/日志。

上游 RAGFlow 的默认部署密码、安全升级及源文档权限由部署者单独负责。不要照搬陌生 compose 文件里的默认密码。下载模型固定 revision 并校验 SHA-256，不启用 trust_remote_code。
