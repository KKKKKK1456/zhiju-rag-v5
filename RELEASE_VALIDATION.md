# 分享包验证范围

版本：v5-experience-20260924.1-share。

本分享包从当前体验版的必需源码白名单导出，没有复制原工作目录、历史 Git 提交或数据库。
移植改动集中在运行配置、启动入口和打包；问答/计算/排序算法未作针对测试题的变更。

已执行的验证：

- 30 项离线测试通过：中文数值、条件更正后的计算输入、证据缺失拒绝、配置边界、Docker 参数白名单、动态 worker 语法、前端交互、发布检查和共享模型路径。
- JavaScript 语法与无持久化/无 HTML 注入的界面契约检查通过。
- 源码允许列表 + 常见密钥/凭据形式、个人目录、原私有 ID 模式扫描未发现命中；只输出命中文件/规则，不泄露匹配值。
- RAGFlow 补丁在临时重建的固定基线文件上干净应用，应用结果与经过审查的源文件一致，Python 语法通过。
- 压缩包包含逐文件 SHA-256 清单，生成后逐字节读回核对。

未完成且不宣称完成：

- 朋友电脑的全新 Docker/RAGFlow/模型安装及端到端问答。
- 原 7000 多份资料的跨机器导入、检索一致性和云端答案复测。
- 全库准确率、90% 等开放问题表现、生产级并发与公网安全认证。

本次打包没有发送知识库原文或聊天给云模型，没有上传原始文件、索引、缓存和密钥。
历史体验版的完整回归数量不作为本分享包的新环境验收结果。
